# ======================================================================
# 🇾🇪 YemenJPT Platform Environment Configuration (V15.0)
# ======================================================================
# INSTRUCTIONS:
# 1. Copy this file to '.env' (`cp .env.example .env`).
# 2. Fill in ALL required values. Use a password manager for strong passwords.
# 3. This file is used by the install.sh script to configure the entire ecosystem.

# --- Domain & Server IP (Required) ---
# The main domain for the platform.
DOMAIN=ph-ye.org
# The public IPv4 address of your server.
SERVER_IP=123.45.67.89

# --- Cloudflare Credentials (Required for Automatic SSL) ---
# Your Cloudflare account email.
CF_EMAIL=your_cloudflare_email@example.com
# Your Cloudflare API Token (scoped to your zone with DNS:Edit permissions).
CF_TOKEN=your_cloudflare_api_token
# Your Cloudflare Zone ID for the domain above.
CF_ZONE_ID=your_cloudflare_zone_id

# --- Unified Passwords (Required) ---
# A strong, unique password used for multiple internal services (TYPO3 admin, ERPNext admin, etc.)
UNIFIED_PASS=generate_a_strong_password
# A strong, unique password for MariaDB root user (for TYPO3, Ghost, Ushahidi, ERPNext databases)
MARIADB_ROOT_PASSWORD=generate_another_strong_password
# A strong, unique password for the Ushahidi database user.
USHHAIDI_DB_PASSWORD=generate_a_third_strong_password
# A strong, unique password for the ERPNext database user.
ERPNEXT_DB_PASSWORD=generate_a_fourth_strong_password

# --- AI Services (Optional) ---
# Your Google Gemini API Key. Required for the cloud AI provider.
API_KEY=your_google_gemini_api_key_here

# --- Telegram Bot for Notifications (Optional) ---
# Your Telegram Bot Token from BotFather.
TELEGRAM_BOT_TOKEN=
# The chat ID of the user/group to receive system notifications.
TELEGRAM_ROOT_CHAT_ID=
