# ======================================================================
# 🇾🇪 YemenJPT Core Application Environment Configuration
# ======================================================================
# INSTRUCTIONS:
# 1. Copy this file to a new file named '.env'.
# 2. Fill in all required values below.
# 3. Use a password manager to generate strong, random passwords.

# --- PostgreSQL Database (Required) ---
# Choose a username for the database.
POSTGRES_USER=yemenjpt_user
# Generate a strong, unique password for the database user.
POSTGRES_PASSWORD=your_strong_password_here
# The name of the database.
POSTGRES_DB=yemenjpt_db

# --- AI Services (Optional) ---
# Your Google Gemini API Key.
# Required if you want to use the cloud AI provider. The app will work without it by using the local AI simulation.
API_KEY=your_google_gemini_api_key_here
